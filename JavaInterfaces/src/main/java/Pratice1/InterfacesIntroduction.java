package Pratice1;

public interface InterfacesIntroduction {//equal to <---public abstract interface InterfacesIntroduction -->
    /*
    * Interface defines a way to define a contract a class must follow as a set of instructions
    *
    * Advantages :
    *
    * Why interfaces:
    *
    * static fields, abstract methods, default methods, private methods  and static methods
    *
    * interface to interface extends keyword is used
    * class to interface implements keyword(implementing class must override all the methods declared in the interface otherwise the
    * must be declared as abstract class)
    *
    * */
}
